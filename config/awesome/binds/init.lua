return {
   client = {
      key = require'binds.client.key',
      mouse = require'binds.client.mouse',
   },
   global = {
      key = require'binds.global.key',
      mouse = require'binds.global.mouse',
   },
   mod    = require'binds.mod'
}

